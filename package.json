{
  "name": "dustopia-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker for dustopia.xyz — Alchemy proxy + R2 atlas/preview/config cache.",
  "main": "worker.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev":    "wrangler dev"
  },
  "dependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
