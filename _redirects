# Cloudflare Pages: serve index.html for /embed and /embed/<addr> so the
# JS-side router can handle both clean URL forms. Status 200 = rewrite,
# the URL bar keeps showing /embed/<addr>.
/embed       /index.html   200
/embed/*     /index.html   200
